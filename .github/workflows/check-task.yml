name: check tasks

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      TASKS_ROOT: "../"
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      - name: build
        working-directory: test
        run: go build -o ./.. ./check_task
      - name: check tasks
        run: ./check_task
